
import { ProxyState } from "../AppState.js";

export class Racer{
  constructor(name, img, number, distance){
    this.name = 'name',
    this.img = 'img', 
    this.number = number ,
    this.distance = distance

  }
}